---
import { Image } from 'astro:assets';
import ImageSlot from './ImageSlot.astro';
import { tagPillStyle, type NewsTag } from '../lib/news';

interface Props {
	href: string;
	image?: string | null;
	imageFocalX?: number | null;
	imageFocalY?: number | null;
	imageLabel: string;
	tags?: NewsTag[];
	title: string;
	excerpt: string;
	dateLabel: string;
	yearLabel: string;
	tinaFieldAttr?: string;
}

const { href, image, imageFocalX, imageFocalY, imageLabel, tags = [], title, excerpt, dateLabel, yearLabel, tinaFieldAttr } = Astro.props;
const focalStyle = `--focal-x:${imageFocalX ?? 50}%;--focal-y:${imageFocalY ?? 50}%;`;
---

<article class="news-card" data-tina-field={tinaFieldAttr}>
	<a href={href} class="news-link">
		{image ? (
			<Image src={image} alt={imageLabel} width={400} height={170} quality="high" class="news-image" style={focalStyle} />
		) : (
			<ImageSlot label={imageLabel} class="news-image" />
		)}
		<div class="news-body">
			{tags.length > 0 && (
				<div class="news-tags">
					{tags.map((tag) => (
						<span class="news-tag" style={tagPillStyle(tag.color)}>{tag.label}</span>
					))}
				</div>
			)}
			<h3 class="news-title">{title}</h3>
			<p class="news-excerpt">{excerpt}</p>
			<div class="news-date">{dateLabel} · {yearLabel}</div>
		</div>
	</a>
</article>

<style>
	.news-card {
		background: #fff;
		border-radius: var(--shape-large);
		overflow: hidden;
		box-shadow: var(--elevation-1);
		transition: transform var(--motion-duration-medium) var(--motion-easing-emphasized), box-shadow var(--motion-duration-medium) var(--motion-easing-emphasized);
	}

	.news-card:hover {
		transform: translateY(-6px) scale(1.01);
		box-shadow: var(--elevation-3);
	}

	.news-link {
		display: block;
		color: inherit;
	}

	.news-card :global(.news-image) {
		width: 100%;
		height: 216px;
		object-fit: cover;
		object-position: var(--focal-x, 50%) var(--focal-y, 50%);
	}

	.news-body {
		padding: 24px;
	}

	.news-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 6px;
		margin-bottom: 14px;
	}

	.news-tag {
		display: inline-block;
		background: var(--tag-bg, rgba(212, 19, 103, 0.1));
		color: var(--tag-color, var(--color-pink));
		font-size: 11px;
		font-weight: 700;
		letter-spacing: 0.5px;
		text-transform: uppercase;
		padding: 5px 12px;
		border-radius: var(--shape-small);
	}

	.news-title {
		font-family: var(--font-heading);
		font-weight: 700;
		font-size: 19px;
		color: var(--color-navy);
		line-height: 1.3;
		margin: 0 0 10px;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	.news-excerpt {
		font-size: 14px;
		color: var(--color-text);
		line-height: 1.55;
		margin: 0 0 16px;
		display: -webkit-box;
		-webkit-line-clamp: 3;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}

	.news-date {
		font-size: 12px;
		font-weight: 600;
		color: var(--color-muted);
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}
</style>
