{
  "version": "v1",
  "hooks": [
    {
      "name": "Astro type-check on .astro save",
      "trigger": "PostFileSave",
      "description": "Runs astro check on every .astro/.ts/.tsx save to catch type errors early, including the TinaCMS Collection annotation gotcha.",
      "matcher": "\\.(astro|ts|tsx)$",
      "action": {
        "type": "command",
        "command": "cd /home/gorlix/Documents/Git/Rotaract2050-website && npx astro check --minimumSeverity error 2>&1 | tail -20",
        "timeout": 60
      }
    }
  ]
}