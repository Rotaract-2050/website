---
import BaseLayout from '../layouts/BaseLayout.astro';
import EmptyView from '../views/EmptyView.astro';
import { translations } from '../data/copy';

const lang = 'it';
const t = translations[lang];
---

<BaseLayout lang={lang} current="commissioni" title={`${t.pagesEmpty.commissioni.title} · Rotaract Distretto 2050`}>
	<EmptyView lang={lang} pageKey="commissioni" />
</BaseLayout>
